name: 🎭 E2E Tests on Deploy

on:
  deployment_status:

jobs:
  check-and-run-e2e:
    name: Check Deployments & Run E2E
    runs-on: ubuntu-latest
    if: github.event.deployment_status.state == 'success'
    steps:
      - name: 📋 Log Current Deployment
        run: |
          echo "=========================================="
          echo "🚀 NEW DEPLOYMENT SUCCESS"
          echo "=========================================="
          echo "Environment: ${{ github.event.deployment.environment }}"
          echo "URL: ${{ github.event.deployment_status.environment_url }}"
          echo "SHA: ${{ github.event.deployment.sha }}"
          echo "Creator: ${{ github.event.deployment.creator.login }}"
          echo "=========================================="

      - name: 🔍 Check Both Deployments
        id: check-deployments
        uses: actions/github-script@v7
        with:
          script: |
            const sha = '${{ github.event.deployment.sha }}';
            console.log(`\n🔍 Checking deployments for SHA: ${sha}\n`);
            
            // Get all deployments for this SHA
            const { data: deployments } = await github.rest.repos.listDeployments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: sha
            });
            
            let vercelUrl = null;
            let hasRailway = false;
            
            // Check each deployment's status
            for (const dep of deployments) {
              const { data: statuses } = await github.rest.repos.listDeploymentStatuses({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: dep.id
              });
              
              const latestStatus = statuses[0];
              if (!latestStatus || latestStatus.state !== 'success') {
                continue;
              }
              
              const url = latestStatus.environment_url || '';
              const creator = dep.creator.login;
              
              // Detect platforms
              if (url.includes('vercel.app') || creator === 'vercel[bot]') {
                vercelUrl = url;
                console.log(`✅ Vercel deployed: ${url}`);
              } else if (url.includes('railway.app') || creator === 'railway-app[bot]') {
                hasRailway = true;
                console.log(`✅ Railway deployed: ${url}`);
              }
            }
            
            if (vercelUrl && hasRailway) {
              console.log('🎉 BOTH DEPLOYMENTS READY - RUNNING E2E TESTS!');
              core.setOutput('run-e2e', 'true');
              core.setOutput('vercel-url', vercelUrl);
            } else {
              console.log('⏳ Waiting for both deployments...');
              console.log(`Vercel: ${vercelUrl ? '✅' : '❌'}`);
              console.log(`Railway: ${hasRailway ? '✅' : '❌'}`);
              core.setOutput('run-e2e', 'false');
            }

      - name: 📥 Checkout Code
        if: steps.check-deployments.outputs.run-e2e == 'true'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.deployment.sha }}

      - name: 🟢 Setup Node.js
        if: steps.check-deployments.outputs.run-e2e == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: 📦 Install Dependencies
        if: steps.check-deployments.outputs.run-e2e == 'true'
        run: npm ci

      - name: 🎭 Install Playwright
        if: steps.check-deployments.outputs.run-e2e == 'true'
        run: npx playwright install chromium --with-deps

      - name: 🧪 Run E2E Tests
        if: steps.check-deployments.outputs.run-e2e == 'true'
        run: |
          echo "=========================================="
          echo "🎯 RUNNING E2E TESTS"
          echo "=========================================="
          echo "Vercel URL: ${{ steps.check-deployments.outputs.vercel-url }}"
          echo "=========================================="
          
          npx nx e2e web-e2e
        env:
          VERCEL_URL: ${{ steps.check-deployments.outputs.vercel-url }}
          DATABASE_URL: ${{ secrets.DEV_DATABASE_URL }}

          CI: true

      - name: 📊 Upload Test Results
        if: steps.check-deployments.outputs.run-e2e == 'true' && always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-results-${{ github.event.deployment.sha }}
          path: |
            apps/web-e2e/playwright-report/
            apps/web-e2e/test-results/
          retention-days: 30

      - name: 💬 Comment on PR
        if: steps.check-deployments.outputs.run-e2e == 'true' && always()
        uses: actions/github-script@v7
        with:
          script: |
            // Find PR for this SHA
            const sha = '${{ github.event.deployment.sha }}';
            const { data: prs } = await github.rest.repos.listPullRequests({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open'
            });
            
            const pr = prs.find(p => p.head.sha === sha);
            if (!pr) {
              console.log('No PR found for this SHA');
              return;
            }
            
            const success = '${{ job.status }}' === 'success';
            const icon = success ? '✅' : '❌';
            const status = success ? 'PASSED' : 'FAILED';
            
            const comment = `## ${icon} E2E Tests ${status}
            
            **Vercel:** ${{ steps.check-deployments.outputs.vercel-url }}
            **Railway:** Deployed ✅
            
            ${success 
              ? '🎉 All E2E tests passed against the deployed stack!' 
              : '⚠️ Some tests failed. Check the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details.'}`;
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              body: comment
            });